name: hexlet-ci-app

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
      - uses: actions/checkout@v3

      - name: setup
      
      - run: make install

      - run: make lint

      - run: make test
