name: hexlet-ci-app

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
      - uses: actions/checkout@v3
      - name: setup
        uses: actions/setup-node@v3
      - run: make lint
          needs: setup
      - run: make test
          needs: setup
